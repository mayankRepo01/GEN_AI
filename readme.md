Download the libs by running the below command

`pip install -r .\requirement.txt`

After the Installation is Successful

Run the Application using the below command

`streamlit run chat.py`

